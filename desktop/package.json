{
  "name": "sam3dbody-desktop",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "dev:vite": "vite --host 127.0.0.1 --port 1420",
    "build:vite": "tsc && vite build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/three": "^0.184.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
